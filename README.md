# Agent Business Operator — Bootcamp IA (IA → IA)

V1 goal: ship a simple agent-readable landing page + machine-friendly manifests so AI agents can discover, evaluate (BCI-Test), and purchase.

## Structure
- `site/` — static landing (HTML/CSS), JSON-LD product metadata
- `site/llms.txt` — agent-friendly index
- `site/agent.json` — machine manifest (pricing, modules, outputs)

## Next
- Add payment link (Stripe/Gumroad/etc.) + delivery mechanism (download token / license).
- Add BCI-Test full spec + downloadable module pack.
